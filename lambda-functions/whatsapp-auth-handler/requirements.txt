# WhatsApp Auth Handler Lambda Dependencies
# Python 3.12 compatible

# PostgreSQL driver
psycopg2-binary>=2.9.9

# AWS SDK (already available in Lambda runtime, but pinned for local testing)
boto3>=1.34.0
